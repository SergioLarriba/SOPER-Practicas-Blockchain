#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>


int main(int argc, char **argv) {
    /* int init, end;
    init = clock();

    while (((end - init)/CLOCKS_PER_SEC) < 10) {
        end = clock();
    }
    */

   sleep(10);

    return 0;
}